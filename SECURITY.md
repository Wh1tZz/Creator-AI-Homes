# Security Policy

## 支持范围

Creator-AI-Homes 目前处于早期开发阶段，安全更新将优先应用到 `main` 分支的最新版本。

由于本项目涉及个人网站、AI API 接入示例、服务端 API 代理和环境变量配置，因此安全性是项目维护中的重要部分。

---

## 安全目标

本项目希望帮助独立创作者更安全地搭建自己的 AI / 自媒体个人网站，重点关注以下安全问题：

* API Key 是否被暴露到前端
* `.env`、`.env.local` 等敏感配置是否被错误提交
* 服务端 API 代理是否存在滥用风险
* CORS 配置是否过于宽松
* 用户输入是否经过基本校验
* 依赖包是否存在已知漏洞
* 部署配置是否可能导致敏感信息泄露

---

## 报告安全问题

如果你发现了安全漏洞，请不要直接在公开 Issue 中披露完整细节。

你可以通过以下方式联系维护者：

* 在 GitHub 上创建 Security Advisory
* 或通过仓库维护者主页提供的联系方式联系我

报告时请尽量包含以下信息：

* 漏洞描述
* 复现步骤
* 可能影响范围
* 相关文件或代码位置
* 建议修复方式，如果有的话

---

## 敏感信息说明

请不要在 Pull Request、Issue 或代码示例中提交以下内容：

* API Key
* Token
* 私有环境变量
* `.env` 或 `.env.local`
* 私有部署配置
* 真实用户数据
* 任何未脱敏的敏感信息

---

## 维护者响应

我会尽快查看安全报告，并优先处理可能导致以下风险的问题：

* API Key 泄露
* 用户数据泄露
* 服务端接口被滥用
* 第三方 API 被异常调用
* 部署环境暴露敏感配置

由于本项目目前由个人维护，响应时间可能不固定，但安全问题会被优先处理。

---

## 安全改进计划

后续计划逐步完善：

* 增加 API 代理输入校验
* 增加依赖漏洞检查
* 增加部署安全清单
* 完善环境变量使用说明
* 使用 Codex 辅助进行安全审查
* 添加更清晰的安全贡献流程
